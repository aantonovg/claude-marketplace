[project]
name = "libreoffice-live-bridge"
version = "1.0.0"
description = "Stdio MCP bridge to the LibreOffice MCP extension's HTTP API on localhost:8765"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.28.0",
    "mcp[cli]>=1.10.0",
]
